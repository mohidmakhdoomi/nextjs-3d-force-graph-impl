I need to review Phase 4 (Fix qualification) — this is primarily about running qualification campaigns and recording evidence. Let me read the key evidence and implementation files.Now let me read the actual test changes and a couple more evidence logs to verify completeness.Let me read the `pickBackgroundDragPoint` implementation and verify the key invariants haven't been violated.Now let me verify the CI workflow is untouched and check the `nodeOccupancyAtPoint` probe implementation:Let me check one more thing — the status file and verify there are no code changes in Phase 4 itself (it should be evidence-only):I've reviewed all the Phase 4 evidence and implementation. Here's my assessment:

## Phase 4 Review: Fix Qualification (Both Engines, Both Rendering Paths)

### Spec/Plan Adherence — FR5 Deliverables

| Deliverable | Status | Evidence |
|---|---|---|
| Targeted Firefox `:224` ×≥60 (SwiftShader) | ✅ **60/60 green** | `phase4-1-ff224-swift-x60.log` — 16.4m total |
| ≥3 full two-engine serial SwiftShader runs | ✅ **3/3 × 22/22** | `phase4-5-clean-validate.log`, `phase4-3-smoke-run{2,3}.log` |
| ≥3 full two-engine GPU-lane runs + renderer evidence | ✅ **3/3 × 22/22** | `phase4-4-gpu-run{1,2,3}.log` — all RTX 3080 verified |
| `npm run validate` green on final tree | ✅ **EXIT 0** | Clean detached-HEAD worktree proof (`phase4-5-clean-validate.log`) |
| Chromium green throughout | ✅ | All 7 runs include chromium, zero failures |
| All banners/per-test verbatim | ✅ | Every run has full per-test output in its log |

### Key Invariants Verified

- **`MOTION_FLOOR = 1`** — unchanged at `matrix.spec.ts:22`, asserted at `:309`.
- **`retries: 0` local** — unchanged at `playwright.config.ts:130` (`process.env.CI ? 2 : 0`).
- **CI workflow** — `git diff HEAD -- .github/workflows/validation.yml` produces empty output (byte-for-byte untouched).
- **Real background drag** — the test at `:225` still issues `mouse.down() → mouse.move(+300, -200, {steps:12}) → mouse.up()` with no retries and no programmatic camera manipulation.

### Evidence Quality

Excellent. The qualification campaign is well-structured:

1. **Driver script** (`phase4-qualify.sh`) — gated: step 1 must pass before burning time on steps 2–4. The `set -u` and the gate check show defensive engineering.
2. **Summary** (`phase4-summary.md`) — clear table mapping every run to its log, renderer evidence, and the `npm run validate` disposition.
3. **Clean-checkout proof** — correctly follows `lessons-critical` Toolchain-and-Worktree-Hygiene: the in-worktree `validate` failure from the untracked `.claude/hooks/worktree-write-guard.cjs` is diagnosed as environment noise (absent from clean checkouts, absent from `git ls-files`), and the gate is proven green on a real `git worktree add --detach HEAD` + `npm ci` instead of being suppressed in committed config. This is exactly the right handling.

### One Noted Adaptation (Non-Blocking)

The plan says targeted repetition "on the SwiftShader path **and** the native-GPU lane." The builder did ×60 targeted on SwiftShader but relied on 3 full GPU-lane runs (×3 `:224` on hardware) because the GPU-lane wrapper (`e2e-gpu-lane.mjs`) doesn't support per-test `--repeat-each` passthrough. This is documented honestly in the summary. The 3 GPU-lane runs are the "historical highest-rate regime" per the spec (the flake reproduced at ~33% rate in #41's GPU-lane parallel campaign), making 3 serial full-suite runs a meaningful qualification. Combined `:224` volume: 66 executions (60+3+3), zero below floor.

### Renderer Evidence (Hardware Control)

All three GPU-lane runs show the exact verified renderer strings:
- Chromium: `ANGLE (Microsoft Corporation, D3D12 (NVIDIA GeForce RTX 3080), OpenGL 4.6)`
- Firefox: `D3D12 (NVIDIA GeForce RTX 3080)`

This is the same hardware on which the flake originally survived (#52), now green ×3.

---
VERDICT: APPROVE
SUMMARY: Phase 4 delivers a thorough, well-documented qualification: 66/66 green `:224` executions across both engines and both rendering paths at retries:0, with clean-checkout validate proof and verified hardware renderer evidence — all invariants preserved.
CONFIDENCE: HIGH
---

KEY_ISSUES:
- None