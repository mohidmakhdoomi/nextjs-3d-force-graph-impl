# Project 7 builder thread

- Entered SPIR `specify` iteration 1 in strict mode.
- The requested spec did not exist in the spawned worktree. Porch therefore requires initial stakeholder clarification before drafting.
- The issue contains no `Baked Decisions` section. Its fixed scope is a Node 22/npm reproducibility baseline, direct validation commands, Playwright smoke coverage, audit evidence, and an automated execution path without dependency/framework modernization.
- Architect confirmed the exact-patch policy, GitHub Actions, evidence-only audits, scope boundaries, and required canvas/control coverage.
- Verified the current Node 22 Maintenance LTS baseline from official Node.js sources as Node `22.23.1` with bundled npm `10.9.8`; drafted the initial specification around that exact toolchain.
- Specify iteration 1 consultation completed after waiting for the mandatory Claude quota reset: Gemini APPROVE, Codex REQUEST_CHANGES, Claude COMMENT.
- Incorporated all feedback: `main` CI trigger, `.nvmrc`, stable audit artifacts plus durable review format, explicit UI readiness/WebGL evidence, generated-output lint ignores, named scripts, advisory-only Corepack/engine policy, and fail-fast validation semantics.
- Architect approved the specification; entered SPIR `plan` iteration 1.
- Drafted three implementation phases for (1) exact toolchain/direct commands, (2) production Playwright WebGL smoke/unified validation, and (3) GitHub Actions/audit artifacts/documentation. All phases are atomic commits in one PR.
- Plan iteration 1 consultation: Gemini APPROVE, Codex REQUEST_CHANGES, Claude APPROVE.
- Accepted Codex's browser-install correction: `npm ci` installs Playwright tooling but not Chromium, so Phase 2 now includes `browser:install` as an explicit local prerequisite and CI uses the `--with-deps` variant.
- Architect approved the plan; entered implementation phase `toolchain_commands`.
- Installed/activated exact Node `22.23.1` with bundled npm `10.9.8`. Pre-change baseline passed build, legacy `next lint`, and direct `tsc --noEmit`; audits reported 14 full findings (2 low, 6 moderate, 5 high, 1 critical) and 9 production findings (4 moderate, 4 high, 1 critical).
- Implemented `.nvmrc`, exact manifest metadata, direct lint/typecheck/audit scripts, generated-output ESLint ignores, lockfile-v3 root engine metadata, and a Node test covering the toolchain/config contract. Added the minimal `test` script required by porch for this configuration-only phase.
- Phase 1 validation: clean `npm ci`, 4/4 Node tests, direct ESLint, typecheck, and production build pass; all 428 pre-existing lock package entries are byte-equivalent in version/resolved/integrity metadata. Audit advisory exit status remains 1 by design and totals are unchanged.
- Phase 1 consultation: Gemini APPROVE, Codex REQUEST_CHANGES, Claude APPROVE. Codex correctly noted `.nvmrc` was untracked and therefore outside the canonical review diff; explicitly added `.nvmrc` and the new contract test to the Git index for re-verification.
- Phase 1 iteration 2 received unanimous Gemini/Codex/Claude approval. Porch advanced to `browser_smoke`; recorded Phase 1 as completed with its command, audit, dependency-integrity, consultation, and small test-infrastructure deviation evidence.
- Phase 2 pins `@playwright/test` `1.61.1`, adds an explicit Chromium install command, and runs the smoke against `next start` after a production build. The exact-version lock delta contains only Playwright, Playwright Core, the Playwright test package, and Playwright's platform-optional `fsevents`.
- The smoke observes a non-zero WebGL drawing buffer, verifies the canvas and accessible controls, exercises axes/reset/rotation transitions, and fails on captured application `console.error` and `pageerror` events. It isolates only the two external Vercel instrumentation scripts that are unavailable on the local production server; application error channels remain strict.
- SwiftShader's continuously animating canvas made the initial Playwright actionability check impractical. The test now proves the initial pause control's center receives pointer events before its sole forced click; after rotation pauses, all remaining interactions use ordinary Playwright clicks. The WebGL read is deferred until after all control interactions.
- Phase 2 validation passes under Node `22.23.1`/npm `10.9.8`: browser installation, lint, typecheck, 5/5 Node contract tests, production build/start, smoke, and unified `npm run validate`. A clean `npm ci` preserves the manifest/lock hashes, and a synthetic probe independently confirmed both browser error-capture channels.
- Current work is preparing the Phase 2 implementation for its mandatory three-way consultation. No blocker is open; the external-script isolation and forced-click rationale are the main review risks and were explicitly surfaced to the architect.
- Architect accepted narrowly scoped isolation of the two unavailable Vercel instrumentation routes, provided it cannot mask application errors. Per the architect's click guidance, only the initial rotation-pause click remains forced, and it is now preceded by a center-point `elementFromPoint` proof that the control receives pointer events; every interaction after pausing rotation uses ordinary Playwright actionability. Two reruns of this stricter smoke passed and cleaned up the server.
- Phase 2 consultation iteration 1 currently has Gemini and Codex approvals. Claude hit its usage quota and reports a 22:40 Toronto reset, so strict porch remains at the mandatory missing-Claude task; no review is being skipped.
- After the quota reset, the mandatory Claude review completed with APPROVE/high confidence. Combined with the preserved Gemini and Codex approvals, porch synthesized unanimous Phase 2 approval and advanced to `ci_documentation`. Phase 2 is recorded complete; its named atomic commit is being created before any Phase 3 implementation.
- Committed Phase 2 atomically as `223241d`, then began `ci_documentation`. Added the pull-request/`main` GitHub Actions path with exact toolchain verification, locked install, contract tests, Chromium system dependencies, unified validation, always-run audit capture, and stable audit/Playwright artifacts.
- Audit evidence normalization is deliberately narrow: a checked-in validator accepts only structurally valid npm audit reports whose severity totals and original exit code agree, while unit tests reject malformed JSON, registry-style error objects, and inconsistent statuses. A validation failure itself has no `continue-on-error`; later evidence steps cannot turn the job green.
- Clean `npm ci` left package and lock hashes unchanged; 12/12 Node tests, lint, typecheck, build, production WebGL smoke, and `npm run validate` pass. Live full/production JSON snapshots validated with original exit 1 and unchanged totals of 14 and 9 findings. `npm explain` produced root-to-package paths for all 14 affected packages for the final review.
- Phase 3 consultation received unanimous high-confidence approval from Gemini, Codex, and Claude with no requested changes. Porch advanced to Review; the plan records the workflow, audit-evidence tests, documentation, clean matrix, and audit-path evidence as complete, and the named Phase 3 commit is being created before review work begins.
