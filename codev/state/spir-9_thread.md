# Project 9 builder thread

- Entered strict SPIR `specify` iteration 1 on 2026-07-18.
- The requested spec file was not present at spawn; `porch next 9` directed creation of the initial specification and requires clarifying input before drafting.
- The issue already fixes the upgrade scope, target package group, validation surface, audit analysis, PostCSS residual disposition, and rollback contract. Clarification is limited to repository-specific ownership and evidence expectations rather than reopening those constraints.
- Architect confirmed Chromium-only validation, review-document-only durable audit analysis, the three stated stakeholder groups, and the fixed Next 15 / React 19 decision.
- Initial specification drafted after inspecting the application boundaries, current manifest/lockfile, existing automated smoke, full documented interaction matrix, issue/research roadmap, related issues, registry metadata, and existing work history.
- Specify iteration 1 consultation: Codex approved with high confidence; Gemini was recorded as a non-blocking skipped lane because Antigravity returned no output. Claude failed twice because the account limit is exhausted until 09:00 America/Toronto. Porch correctly remains on the missing Claude consultation; no gate or phase was bypassed.
- Resumed on 2026-07-18 after the architect requested another Claude attempt. The Claude specification review completed with an APPROVE verdict and high confidence. `porch next 9` now reports the `spec-approval` gate pending; work is stopped for human approval.
- Architect review found the specification content acceptable but identified a stale Consultation Log entry. Updated it to record the completed Codex and Claude approvals and Gemini's no-output skip before re-requesting `spec-approval`.
- The architect required an actual Gemini result rather than a skipped lane. The retried Gemini consultation approved with high confidence and suggested making exact pins explicit for all six target packages. Incorporated that clarification into the specification and updated the Consultation Log to record all three approvals.
- Human `spec-approval` advanced the project into Plan iteration 1. Drafted a two-phase plan: first reverify and atomically update the six-package manifest/lock group with durable dependency contracts and path-level graph/audit inspection; then qualify clean installation, static/build/start/smoke gates, the full manual Chromium interaction matrix, and residual-risk evidence. Both phases remain commits in one PR.
- Plan iteration 1 review: Claude and the eventually successful Gemini retry approved with high confidence. Codex endorsed the plan with a COMMENT requesting explicit inspection of React Compiler configuration surfaces. Updated Phase 2 to inspect `next.config.js`, `package.json`, and any present Babel configuration, and recorded all three results before re-requesting `plan-approval`.
- Human `plan-approval` advanced the project into Phase 1 (`dependency_baseline`). The rollback baseline is `d88b1a1fb345b9976ea0d8dc520f10e76e84be3e`; Node `v22.23.1`, npm `10.9.8`, `.nvmrc`, and `packageManager` all matched before dependency edits.
- A clean pre-change `npm ci` established a valid installed-tree snapshot. Both audit reports validated with their original exit 1: full `14` affected packages (`2` low, `6` moderate, `5` high, `1` critical) and production `9` (`4` moderate, `4` high, `1` critical).
- Implementation-time npm registry checks found no target drift: Next and its plugin `15.5.20` remain the `backport` tag, React/DOM `19.2.7` remain stable `latest`, the selected types are current, Node satisfies the declared engines, and all selected peer ranges accept their exact counterparts.
- Updated only the six planned exact manifest entries and regenerated lockfile v3 with npm `10.9.8`. A fresh plain `npm ci` passed without peer warnings or bypass flags; `npm ls` reports no problems, one locked React runtime at `19.2.7`, matching React DOM, matching Next/plugin, scheduler `0.27.0`, and `csstype` `3.2.3`.
- Full lock inspection found 6 added, 7 removed, and 41 changed package records, all attributable to the target group: Next/SWC/env, React/scheduler/types/csstype, and Next's optional Sharp range/subtree. No retained package changed major, no non-npm registry source appeared, and all eight expected platform SWC records align at `15.5.20`.
- The audits remain intentionally nonzero and structurally valid. All 20 direct Next advisories in the baseline are absent at `15.5.20`, including the prior critical finding. The unchanged PostCSS advisory still covers both root `postcss@8.5.1` and `next@15.5.20 > postcss@8.4.31`; npm's affected-package totals therefore read full `17` and production `12` because it now propagates the Next/PostCSS residual through optional Next peer dependents. Raw JSON and detailed advisory/lock comparisons remain untracked under `/tmp/spir-9-evidence-20260718T140916`.
- The upgraded build passes. Next `15.5.20` emits a builder-environment-only workspace-root warning because this isolated worktree is nested beneath the main checkout and both contain lockfiles; it selects the parent for output-file tracing. No repository configuration was changed for that worktree artifact, and a normal checkout/CI has only its own root lockfile.
