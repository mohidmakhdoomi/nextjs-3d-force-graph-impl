# Plan: Patch Next 15 and React to the Supported Security Baseline

## Metadata

- **ID**: plan-2026-07-18-next-react-supported-baseline
- **Status**: completed
- **Specification**:
  [`codev/specs/9-patch-next-15-and-react-to-the.md`](../specs/9-patch-next-15-and-react-to-the.md)
- **Created**: 2026-07-18
- **Delivery**: Two dependency-ordered implementation commits in one pull
  request; no phase-level pull requests

## Executive Summary

Implement the specification's selected atomic Next 15 / React 19 backport in
two phases:

1. reverify the six exact artifacts and their compatibility metadata, capture
   the pre-change audit/tree baseline, update `package.json` and
   `package-lock.json` together under Node `22.23.1` / npm `10.9.8`, add a
   narrow dependency contract, and inspect the resulting peer and lock trees;
2. qualify the committed graph through clean installation, static checks,
   production build/start, the existing real-WebGL Chromium smoke, the full
   manual interaction matrix, and final audit-path review.

The manifest and lockfile remain one rollback unit. No application component,
Next 16, 3D, Tailwind, direct PostCSS, lint-stack, or React Compiler change is
planned. Raw audit JSON remains transient local/CI evidence; durable totals,
path-by-path applicability conclusions, and the nested PostCSS disposition
will be written only to the review document during the Review phase.

## Success Metrics

- [x] Implementation-time registry and support checks confirm the six selected
      artifacts, relevant dist-tags, engines, dependencies, and peer ranges, or
      any drift is stopped and resolved with the architect.
- [x] The six direct entries are exact; Next and its ESLint plugin are
      `15.5.20`, React and React DOM are exactly `19.2.7`, and the React type
      packages are `19.2.17` / `19.2.3`.
- [x] `package.json` and lockfile-v3 root metadata agree and change in one
      atomic commit generated by npm `10.9.8`.
- [x] Clean `npm ci` succeeds without peer-bypass flags or invalid/unmet peer
      output.
- [x] The installed and locked graph has one stable React runtime, matching
      React DOM, expected npm registry sources, no unexplained package major,
      and attributable target-group transitive movement.
- [x] Next, `@next/env`, native SWC packages, `styled-jsx`, scheduler, React
      types, `csstype`, and both relevant PostCSS paths are explicitly
      inspected.
- [x] `npm test`, lint, typecheck, build, direct production start, Playwright
      smoke, and aggregate `npm run validate` pass.
- [x] The App Router server/client boundary and dynamic `{ssr: false}` WebGL
      island remain unchanged.
- [ ] All twelve manual Chromium interaction checks pass against the
      production application without page, hydration, console, WebGL, or timer
      errors. Items 1–10 and 12 passed; item 11 remains the explicitly
      baseline-identical environment limitation documented below.
- [x] Before/after full and production audits are compared advisory by
      advisory and path by path without claiming a clean audit.
- [x] The nested `next > postcss@8.4.31` residual is retained and assigned the
      explicit temporary risk disposition required by the specification.
- [x] Rollback restores the manifest, lockfile, and any upgrade-specific
      contract assertion together, followed by a clean `npm ci`.

## Phases (Machine Readable)

<!-- REQUIRED: porch uses this JSON to track phase progress. -->

```json
{
  "phases": [
    {"id": "dependency_baseline", "title": "Apply the Atomic Supported Dependency Baseline"},
    {"id": "production_qualification", "title": "Qualify Production Behavior and Residual Risk"}
  ]
}
```

## Phase Status

| Phase | Status | Planned commit |
| --- | --- | --- |
| Apply the Atomic Supported Dependency Baseline | completed | `[Spec 9][Phase: dependency-baseline] chore: Patch Next and React baseline` |
| Qualify Production Behavior and Residual Risk | completed | `[Spec 9][Phase: production-qualification] test: Qualify supported dependency baseline` |

## Phase Breakdown

### Phase 1: Apply the Atomic Supported Dependency Baseline

**Status**: completed
**Dependencies**: Approved specification  
**Planned commit**:
`[Spec 9][Phase: dependency-baseline] chore: Patch Next and React baseline`

#### Objectives

- Prove that the researched versions remain the appropriate published Next 15
  backport and stable React 19 package group before editing the manifest.
- Update the six exact direct entries and npm lockfile as one reproducible
  dependency unit.
- Make the target-group version and lockfile invariants durable without
  overfitting tests to npm internals.
- Produce a clean, explainable peer/runtime/transitive graph and retain
  validated before/after audit evidence for final review.

#### Deliverables

- [x] Implementation-time metadata record for all six target packages.
- [x] Validated pre-change full and production audit JSON plus dependency-tree
      snapshots retained outside the tracked source diff.
- [x] Atomic `package.json` / `package-lock.json` update generated under the
      exact toolchain.
- [x] Narrow dependency-contract assertions for target versions, exact pins,
      root manifest/lock synchronization, framework/plugin alignment, and the
      single locked React runtime.
- [x] Passing clean install, contract tests, and peer-tree inspection.
- [x] Complete lockfile-diff attribution and validated post-change full and
      production audit evidence ready to distill in the Review phase.

#### Files

- Modify `package.json`.
- Modify `package-lock.json`.
- Modify `tests/toolchain.test.mjs`.
- Update this plan and `codev/state/spir-9_thread.md` only for protocol status
  and evaluation tracking.
- Do not track generated audit JSON, npm-tree snapshots, or registry-response
  dumps.

#### Implementation Details

1. Record the pre-upgrade commit as the rollback baseline. Before any dependency
   edit, confirm:
   - `.nvmrc` is `22.23.1`;
   - `node --version` is `v22.23.1`;
   - `npm --version` is `10.9.8`;
   - `package.json#packageManager` is `npm@10.9.8`; and
   - the worktree has no unexplained tracked dependency-file changes.
2. Before manifest modification, capture:
   - `npm ls --all --json`;
   - `npm audit --json` with its original exit code;
   - `npm audit --omit=dev --json` with its original exit code; and
   - validator output from `scripts/validate-audit-report.mjs` for both audit
     reports.
   Advisory exits are evidence, while malformed JSON or registry/tool failures
   block the phase.
3. Query the registry for every exact target using `npm view` and record
   publication, version, dist-tags, engines, peer dependencies, and material
   direct dependencies. Explicitly inspect:
   - `next@15.5.20` support/backport status, React/DOM peer range,
     `@next/env`, `styled-jsx`, SWC optional dependencies, and nested PostCSS;
   - `@next/eslint-plugin-next@15.5.20`;
   - `react@19.2.7`;
   - `react-dom@19.2.7`, its React peer, and scheduler dependency;
   - `@types/react@19.2.17` and its `csstype` dependency; and
   - `@types/react-dom@19.2.3` and its React-types peer.
   Confirm Node `22.23.1` satisfies every relevant engine and that React
   `19.2.7` is stable. If a selected artifact is unpublished, no longer the
   intended tag/support line, incompatible, or superseded in a way covered by
   the specification's drift rule, stop for architect confirmation rather than
   substitute autonomously.
4. Update only these existing direct entries, preserving dependency
   classification and exact syntax:

   ```text
   dependencies:
     next       15.5.20
     react      19.2.7
     react-dom  19.2.7

   devDependencies:
     @next/eslint-plugin-next  15.5.20
     @types/react              19.2.17
     @types/react-dom          19.2.3
   ```

   Regenerate `package-lock.json` once with npm `10.9.8`. Do not use
   `--force`, `--legacy-peer-deps`, `npm audit fix`, or an `overrides` entry.
5. Extend `tests/toolchain.test.mjs` with behavior-level dependency contracts:
   - the six manifest values equal the verified exact group and contain no
     range operators;
   - React equals React DOM and Next equals its lint plugin;
   - matching root lockfile dependency/devDependency values are already
     covered by the existing root-synchronization assertion;
   - target lock records resolve to the intended versions; and
   - exactly one `node_modules/react` runtime record exists in the lockfile.
   Keep provenance and full transitive attribution as review procedures rather
   than encoding the complete npm graph in brittle tests.
6. Remove the installed tree through the normal clean-install workflow and run
   plain `npm ci`. Inspect `npm ls next @next/eslint-plugin-next react react-dom
   @types/react @types/react-dom postcss --all`; any `invalid`, `UNMET`,
   peer warning, duplicate React runtime, prerelease, or mismatch blocks the
   phase.
7. Review the complete package-lock diff:
   - root manifest metadata and lockfile version;
   - every changed, added, and removed package record;
   - version, resolved URL, integrity, engines, peers, and optional metadata;
   - Next, `@next/env`, `styled-jsx`, every native SWC optional package, and
     `next > postcss`;
   - React, React DOM, scheduler, React types, and `csstype`;
   - all registry sources and introduced majors; and
   - the parent range responsible for every material transitive move.
   Reject unexplained churn or any new git/file/HTTP/unknown registry source.
8. Capture and validate post-change full and production audits with original
   exits. Compare each before/after advisory and every root-to-affected-package
   path, including paths that npm coalesces into one advisory record. Preserve
   the actual `next > postcss@8.4.31` installed path and
   GHSA-qx2v-qp2m-jg93/CVE-2026-41305 for the final review. Do not commit the
   generated JSON or claim that the audit is clean.

#### Acceptance Criteria

- [x] Exact Node/npm checks pass before dependency modification and lockfile
      generation.
- [x] All six target artifacts and their required metadata are reverified at
      implementation time with no unresolved drift.
- [x] Only the six named direct manifest entries change.
- [x] `package.json` and `package-lock.json` are both staged for the same phase
      commit and the lock remains version 3.
- [x] Plain `npm ci` and `npm test` pass without bypass flags.
- [x] `npm ls` exits cleanly with matching intended versions, no invalid/unmet
      peer, and one React runtime.
- [x] Every lockfile change has an attributable target-group parent; sources,
      majors, and native package records are expected.
- [x] Both pre/post full and production audit reports are structurally valid
      and every changed or residual advisory path has a prepared disposition.
- [x] The nested PostCSS residual remains visible without an override.
- [x] App Router and graph component source files are untouched.

#### Test Plan

- **Contract tests**: Run `npm test` under the npm-managed environment so the
  exact toolchain and new dependency assertions execute together.
- **Install integration**: Run clean `npm ci`, then npm tree inspection for the
  six targets, scheduler, `csstype`, and PostCSS.
- **Lockfile analysis**: Compare the pre-upgrade lockfile from Git with the
  regenerated lockfile by package record, source, integrity, peer, engine,
  optional metadata, and major version.
- **Security evidence**: Validate before/after full and production audit JSON
  while retaining original exits; enumerate advisory paths with `npm explain`
  or equivalent installed-tree evidence.
- **Scope review**: Confirm no `app/**`, configuration, unrelated direct
  dependency, classification, or unsupported override changed.

#### Rollback Strategy

Revert the complete Phase 1 commit so `package.json`, `package-lock.json`,
`tests/toolchain.test.mjs`, and protocol tracking return together to the
recorded pre-upgrade commit. Run clean `npm ci` with Node `22.23.1` / npm
`10.9.8` and rerun the baseline contract tests. Never restore or revert only
one dependency file.

#### Risks

- **Risk**: Registry metadata has drifted since the research.
  - **Mitigation**: Reverify immediately before editing and require architect
    confirmation for any target substitution.
- **Risk**: npm rewrites unrelated transitives or sources.
  - **Mitigation**: Use the exact npm version, review every lock record, and
    reject unattributed changes.
- **Risk**: Install success hides a duplicate or invalid peer.
  - **Mitigation**: Combine clean `npm ci`, explicit `npm ls`, lockfile runtime
    counting, and exact version contracts.
- **Risk**: Improved totals obscure an applicable residual.
  - **Mitigation**: Compare advisories path by path and retain PostCSS
    explicitly regardless of aggregate totals.

#### Evaluation Gate

- Present the metadata verification, clean-install/peer results, complete
  lockfile attribution, audit-diff preparation, contract tests, and 3-way phase
  consultation.
- Mark the phase completed in this plan and create the single planned commit
  only after Porch verification permits it.

**Evaluation**: Node `v22.23.1` / npm `10.9.8` registry verification found no
target drift. Plain `npm ci`, the clean peer tree, the single React runtime
contract, tests, and production build passed. All changed lock records were
attributed to Next/SWC/Sharp or React/types support subtrees; no unexpected
major or non-registry source appeared. Validated audits show all 20 prior
direct Next advisories removed while both PostCSS paths remain explicit.
Gemini, Codex, and Claude unanimously approved the completed phase with high
confidence.

---

### Phase 2: Qualify Production Behavior and Residual Risk

**Status**: completed
**Dependencies**: Phase 1 completed and committed  
**Planned commit**:
`[Spec 9][Phase: production-qualification] test: Qualify supported dependency baseline`

#### Objectives

- Prove the committed dependency baseline preserves static correctness,
  production server behavior, real WebGL rendering, and the complete graph
  interaction path.
- Finalize the evidence needed for an honest advisory-by-advisory review and
  explicit residual-risk disposition.
- Avoid application changes unless phase verification exposes a narrowly
  in-scope compatibility defect and Porch returns the phase for a reviewed fix.

#### Deliverables

- [x] A second clean `npm ci` proving the committed manifest/lock graph is
      reproducible and unchanged.
- [x] Passing unit/contracts, lint, typecheck, direct build/start, Playwright
      Chromium smoke, and aggregate validation.
- [x] Source-diff proof that the App Router server components, client wrapper,
      `{ssr: false}` WebGL island, and compiler configuration surfaces remain
      unchanged.
- [ ] Completed twelve-step manual Chromium interaction matrix against
      `next start`, with errors monitored through the sequence.
- [x] Final full/production audit and installed-path verification ready for the
      review document, including the nested PostCSS residual.
- [x] Phase evaluation recorded in this plan without committing generated audit
      JSON.

#### Files

- Modify `eslint.config.mjs` and `tests/toolchain.test.mjs` only to exclude
  Next-generated `next-env.d.ts` from lint traversal and preserve that
  generated-output contract.
- No application, manifest, lockfile, browser test, or other configuration
  change is planned.
- Update this plan and `codev/state/spir-9_thread.md` for phase status and
  evaluation tracking.
- Create `codev/reviews/9-patch-next-15-and-react-to-the.md` only after Porch
  enters the Review phase; audit tables do not belong in this plan.
- If validation reveals a compatibility defect, stop and revise this file list
  and test strategy through the phase review before changing source.

#### Implementation Details

1. Verify Phase 1's named commit exists and the only remaining working-tree
   files are known Porch/builder artifacts. Run clean `npm ci`; confirm it does
   not alter `package.json` or `package-lock.json`.
2. Next `15.5.20` adds a generated `.next/types/routes.d.ts` triple-slash
   reference to ignored `next-env.d.ts`; ensure the flat ESLint configuration
   excludes that generated entry point, while the contract test continues to
   prove that no application source or test path is ignored.
3. Run and record each direct gate:
   - `npm test`;
   - `npm run lint`;
   - `npm run typecheck`;
   - `npm run build`;
   - `npm run start -- --hostname 127.0.0.1 --port 3000`, followed by a
     successful root request and orderly server shutdown;
   - `npm run test:smoke`; and
   - `npm run validate`.
   The Playwright path must use the real production server, Chromium, an
   initialized nonzero WebGL drawing buffer, axes/reset/rotation transitions,
   and strict console/page-error collection.
4. Compare the implementation diff against the rollback baseline and inspect
   `app/page.tsx`, `app/layout.tsx`, and
   `app/components/FocusGraphWrapper.tsx`. Confirm the page/layout remain server
   components, the wrapper remains the client boundary, and the dynamic
   `FocusGraph` import still has `ssr: false`. Also inspect `next.config.js`,
   `package.json`, and any present `.babelrc*` / `babel.config.*` surface.
   Confirm no `reactCompiler` option, `babel-plugin-react-compiler` dependency
   or transform, or other React Compiler-enabling configuration was introduced.
5. Against the production build in Chromium, perform and record the complete
   manual matrix:
   1. visible, populated, nonzero WebGL graph canvas;
   2. pointer interaction disabled initially and enabled after its delay;
   3. observable automatic horizontal rotation;
   4. pause and resume stop/restart rotation;
   5. axes show and hide visibly;
   6. camera reset fits the graph and preserves documented rotation behavior;
   7. wheel zoom works in and out;
   8. background drag rotates through Trackball controls;
   9. node drag fixes the released node and releases the prior fixed node;
   10. node left-click focuses the camera, stops rotation, fixes that node, and
       releases other fixed nodes;
   11. right-click releases the fixed node; and
   12. the canvas remains responsive with no unexpected page, console,
       hydration, WebGL, or timer error.
   Representative nodes are sufficient. Record exactly which mouse-button
   variants were exercised rather than infer unobserved behavior.
6. Rerun the validated full and production audit captures after the final clean
   install and reconcile them with Phase 1 evidence. Prepare the Review-phase
   table with advisory URL/identifier, severity/range, before/after root paths,
   prod/dev classification, delta, exercising mechanism, repository evidence,
   applicability/confidence, and owner/disposition.
7. Confirm the review evidence will state that Next `15.5.20` owns nested
   `postcss@8.4.31`, that the installed path remains in
   GHSA-qx2v-qp2m-jg93/CVE-2026-41305, that no supported Next 15 selection in
   scope removes it, that no blanket override was used, and that the temporary
   residual is tracked upstream/later while issue #10 owns direct
   `postcss@8.5.1`.

#### Acceptance Criteria

- [x] The previous phase commit is present before qualification begins.
- [x] A new clean install reproduces the committed lockfile without mutation,
      warnings, peer bypasses, or duplicate React.
- [x] Every direct validation command and `npm run validate` passes.
- [x] Production start serves the root page and terminates cleanly.
- [x] The automated Chromium smoke observes real WebGL and no unexpected
      console/page errors.
- [ ] All twelve manual interaction checks pass and record only behaviors
      actually exercised.
- [x] Server/client boundaries and `ssr: false` remain unchanged.
- [x] `next.config.js`, `package.json`, and every present Babel configuration
      surface prove React Compiler remains disabled.
- [x] Final audit evidence is valid, path-complete, and honest about all
      residuals.
- [x] No unrelated source, dependency, generated JSON, or cleanup enters the
      implementation diff.

#### Test Plan

- **Unit/contracts**: Existing Node tests, the Phase 1 dependency contract, and
  the generated-output lint-ignore contract including `next-env.d.ts`.
- **Static integration**: ESLint, TypeScript `--noEmit`, clean Next production
  build, explicit source-boundary inspection, and compiler-config inspection.
- **Production integration**: Direct `next start` root response and the existing
  Playwright-managed production WebGL smoke.
- **Manual behavior**: Complete Chromium interaction matrix with representative
  graph nodes and continuous error observation.
- **Security evidence**: Validated full/production audit rerun plus
  root-to-package path reconciliation.
- **Overmocking check**: No graph or framework runtime is mocked; browser
  validation uses the real application and WebGL implementation. Only the
  existing unavailable local Vercel telemetry endpoints remain isolated by the
  smoke.

#### Rollback Strategy

If qualification fails because of the package baseline, revert the entire
Phase 1 dependency commit, including manifest, lockfile, and contract
assertions, then clean-install and rerun the old baseline. Do not selectively
pin a transitive, change the 3D/CSS/lint stack, or retain a new manifest with an
old lockfile. A qualification-only tracking commit may be reverted separately
because it contains no dependency state.

#### Risks

- **Risk**: Static/build checks pass while an imperative graph interaction
  regresses.
  - **Mitigation**: Require both the real-WebGL smoke and the full manual matrix.
- **Risk**: Software WebGL makes browser actions slow or flaky.
  - **Mitigation**: Follow the existing baseline lesson: pause continuous
    rotation before later actions, prove pointer receipt when forcing the first
    pause, and keep later interactions ordinary and observable.
- **Risk**: A compatibility failure tempts unrelated cleanup.
  - **Mitigation**: Stop, classify the failure, and limit any returned fix to
    the approved Next/React boundary; defer 3D, CSS, and lint modernization.
- **Risk**: Final audit output changes due to registry/advisory updates.
  - **Mitigation**: Preserve timestamps and original exits, validate structure,
    reconcile by advisory/path, and distinguish measurement drift from lockfile
    drift.

#### Evaluation Gate

- Present direct command results, production lifecycle evidence, automated and
  manual Chromium outcomes, architecture-preservation proof, audit-path
  readiness, and 3-way phase consultation.
- Mark the phase completed in this plan and create the single planned commit
  only after Porch verification permits it.

**Approved deviation (2026-07-18)**: The original phase expected no
configuration or test change. Direct lint after the upgraded production build
showed that Next `15.5.20` now generates an ignored `next-env.d.ts` containing
a route-types triple-slash reference that flat ESLint nevertheless traversed.
The architect approved adding only that generated root file to the existing
global ignore and exact-ignore contract. Application files, dependencies, and
the lockfile remain unchanged.

**Implementation evidence (2026-07-18)**: The named Phase 1
commit `0c57149` is present. A second plain `npm ci` reproduced identical
manifest/lock hashes and a problem-free peer tree with one React `19.2.7`
runtime. The direct test, lint, typecheck, build, production start/root request,
Playwright smoke, and aggregate `npm run validate` gates passed. The production
server terminated through the expected SIGTERM path. The only build/start
warning is the known isolated-builder warning caused by this worktree and the
parent checkout both containing lockfiles.

The rollback-baseline diff contains no change to `app/page.tsx`,
`app/layout.tsx`, or `app/components/FocusGraphWrapper.tsx`: page/layout remain
server components and the wrapper remains the client boundary with
`dynamic(..., {ssr: false})`. `next.config.js` is still empty, no Babel
configuration exists, and no application/config/package surface enables React
Compiler.

Chromium production verification passed matrix items 1–10 and 12: the populated
nonzero WebGL canvas, delayed pointer enablement, horizontal rotation,
pause/resume, axes, paused/active reset behavior, both wheel directions,
Trackball background drag, successive node drags, and active-state node
left-click behavior were observed with continuous console/page-error
collection. Node left-click required the software-WebGL harness to freeze the
20 ms camera timer between pointer-down and pointer-up so the library's
asynchronous raycast retained the visibly hovered node; the resulting real
handler stopped rotation, focused the camera, fixed the clicked node, and
released the prior node.

Matrix item 11 did **not** pass and is not represented as 12/12. A physical
right-click on a visibly hovered fixed node left `fx`/`fy`/`fz` unchanged in
headless SwiftShader Chromium. The exact same result reproduced from rollback
baseline `fef4da9` with Next `15.1.11` / React `19.0.0`, so it is not upgrade
drift. The bounded headed attempt used the available WSL display but reported
the same SwiftShader renderer and stalled before completing the interaction.
Per the specification's mouse-button caveat, only the variants actually
distinguished are claimed. The architect classified this baseline-identical
environment/pre-existing result as an out-of-scope follow-up and directed the
phase not to change application code or block solely on it.

Final full and production audit JSON validated with original exit 1 and is
byte-identical to Phase 1: full remains 17 affected packages (2 low,
10 moderate, 5 high), production remains 12 (8 moderate, 4 high), and neither
is described as clean. Installed-path evidence reconfirms
`next@15.5.20 > postcss@8.4.31` alongside direct `postcss@8.5.1`; no override
was added, the framework-owned residual remains accepted temporarily, and
issue #10 owns the direct PostCSS update.

**Evaluation**: Gemini and Claude approved the first completed-phase review.
Codex initially requested that the baseline-identical right-click result block
completion; the builder rebutted with the specification caveat and the
architect's explicit scope decision. In the second review, Gemini and Claude
approved with high confidence and Codex returned a high-confidence COMMENT
with no requested code change, agreeing that the implementation is correct and
the plan must continue to avoid a literal 12/12 claim. Porch then advanced the
completed plan to Review.

## Dependency Map

```text
Approved specification
        |
        v
Phase 1: Atomic supported dependency baseline
        |
        v
Phase 2: Production behavior and residual-risk qualification
        |
        v
Review document and single pull request
```

## Resource Requirements

### Development Environment

- Exact Node.js `22.23.1` and npm `10.9.8`.
- npm registry access for metadata, installation, and audit queries.
- Chromium installed through the existing Playwright command.
- Linux-compatible software WebGL support using the existing Playwright
  configuration.

### Infrastructure

- No database, service, API, schema, deployment, or monitoring change.
- Existing GitHub Actions validation and audit/Playwright artifact channels are
  reused without modification unless implementation evidence proves a
  specification gap.

## Integration Points

### External Systems

- **npm registry and advisory service**
  - **Phase**: Both phases.
  - **Use**: Artifact metadata, exact tarballs, peer/dependency resolution, and
    audit evidence.
  - **Failure policy**: Registry errors or malformed audit output block evidence
    collection; they are not treated as findings or clean results.
- **Chromium via Playwright**
  - **Phase**: Phase 2.
  - **Use**: Production application, real WebGL, control, and error validation.
  - **Failure policy**: Preserve Playwright diagnostics and distinguish
    environment failure from an application regression before making changes.

### Internal Systems

- `package.json` / `package-lock.json`: atomic dependency and rollback contract.
- `tests/toolchain.test.mjs`: durable manifest/lock/toolchain assertions.
- `playwright.config.ts` / `tests/e2e/smoke.spec.ts`: existing production WebGL
  test harness.
- `scripts/validate-audit-report.mjs`: audit evidence validator that preserves
  advisory exits.
- `app/page.tsx`, `app/layout.tsx`, and
  `app/components/FocusGraphWrapper.tsx`: architecture invariants inspected but
  not modified.
- `next.config.js`, `package.json`, and any present Babel configuration:
  compiler-enabling surfaces inspected but not modified.

## Risk Analysis

| Risk | Probability | Impact | Mitigation | Owner |
| --- | --- | --- | --- | --- |
| Target support/tag drift | Low | High | Reverify before editing; require architect decision for substitution | Builder / architect |
| Invalid peer or duplicate React | Low | High | Exact pair, clean install, npm tree, lock contract | Builder |
| Unexplained lockfile churn/source | Medium | High | Exact npm and record-by-record attribution | Builder |
| Production graph regression | Low | High | Static gates, real-WebGL smoke, full manual UX | Builder |
| Known PostCSS residual understated | High | High | Mandatory advisory/path/risk disposition with upstream link | Builder / architect |
| Registry/audit measurement drift | Medium | Medium | Timestamp, validate, and reconcile by advisory/path | Builder |

## Validation Checkpoints

1. **Before manifest edit**: Exact toolchain, rollback commit, baseline audits,
   tree, and all six registry/support/peer checks are valid.
2. **Before Phase 1 commit**: Manifest/lock atomicity, clean install, contracts,
   peer tree, complete lock attribution, and audit comparison are satisfactory.
3. **Before Phase 2 commit**: All direct/aggregate gates and the complete
   production Chromium matrix pass with architecture boundaries preserved.
4. **Before pull request**: Review document contains the durable implementation
   evidence, path-by-path audit table, drift notes, rollback instructions, and
   PostCSS residual disposition; generated JSON is absent from the diff.

## Monitoring and Observability

- No new production metric, log, or alert is introduced by a dependency-only
  backport.
- Existing browser collection of page and console errors and CI audit/Playwright
  artifacts provides validation observability.
- Review-time evidence records command outcomes, audit totals/paths, and manual
  interactions without adding runtime instrumentation.

## Documentation Updates Required

- [x] Update this plan's phase status/evaluation summaries as Porch advances.
- [x] Create
      `codev/reviews/9-patch-next-15-and-react-to-the.md` in Review with version
      verification, lockfile attribution, validation evidence, audit-path
      analysis, residual disposition, and rollback proof.
- [x] Update architecture/lessons documentation only if implementation reveals
      a new durable cross-cutting fact or lesson; a version changelog alone does
      not qualify.
- [x] No README, API, deployment, user-guide, or architecture-diagram change is
      expected because commands and application behavior remain unchanged.

## Post-Implementation Tasks

- [x] Run final full and production audits and reconcile with captured evidence.
- [x] Complete the Review document and systematic lessons check.
- [ ] Run 3-way PR review and address all actionable feedback.
- [ ] Open one pull request containing both phase commits and review evidence.
- [ ] After merge, verify the integrated branch under the protocol's Verify
      phase unless the architect explicitly skips it.

## Consultation Log

### Initial plan review

Gemini and Claude approved the plan with high confidence and found the
two-phase ordering, file references, requirements coverage, validation
strategy, and rollback controls complete. Codex returned a high-confidence
COMMENT that otherwise endorsed the plan but asked that the React Compiler
invariant explicitly cover configuration surfaces, not only application
components. Phase 2 now requires inspection of `next.config.js`, `package.json`,
and any present Babel configuration for React Compiler options, dependencies,
or transforms.

### Post-feedback plan review

No second plan review was requested. The architect approved the plan after the
initial three-way review and the React Compiler configuration-surface
clarification was incorporated.

## Approval

- [x] Specification approved
- [x] Initial expert AI consultation complete
- [x] Technical lead plan approval
- [x] Final expert AI consultation not requested; initial feedback was resolved
      before approval

## Change Log

| Date | Change | Reason | Author |
| --- | --- | --- | --- |
| 2026-07-18 | Initial two-phase implementation plan | Translate the approved atomic dependency and production-qualification requirements into committable work | Builder |
| 2026-07-18 | Added explicit React Compiler configuration inspection and recorded all three reviews | Incorporate Codex feedback and the successful Gemini retry before human approval | Builder |

## Notes

- Delivery is measured by completed phases, and phases do not create autonomous
  pull requests.
- Audit nonzero exits remain diagnostic evidence, not a validation bypass.
- If no compatibility code change is required, Phase 2 intentionally commits
  only protocol evaluation/status changes; its observable value is qualification
  of the real production user path and residual-risk record.
